{%macro CardAbout(title="",firstText="",secoundText="")%}
                 <div class=" border border-black p-8 rounded-xl ">
                    <div class="text-mainColor font-bold text-2xl ">{{title}}</div>
                    <div class="flex justify-start my-4">
                        <div class="flex border-b-2 w-2/12 border-red-600"></div>
                    </div>
                    <div class="my-3 text-secoundColor">
                        {{firstText}}
                    </div>
                    <div class="text-secoundColor">
                    {{secoundText}}
                    </div>
                </div>
{%endmacro%}
{%macro CardArticls(image="",text="",date="")%}
                <div class="rounded-xl shadow-lg ">
                    <img class="rounded-t-xl w-full h-60" src="{{image}}">
                    <div class=" text-mainColor font-semibold text-lg h-24  p-4">{{text}}</div>
                    <div class=" text-start text-gray-400 text-xs p-4">{{date}}</div>
                </div>
{%endmacro%}