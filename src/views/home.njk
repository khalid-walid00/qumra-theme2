{% extends "layouts/layout.njk" %}
{% from "common/components/product.njk" import product %}
{% block title %}متجرك - الصفحة الرئيسية{% endblock %}
{% block content %}
    <div class="bodyMain"> 

    
    <div class="swiper mySwiper">
        <div class="swiper-wrapper">
            <div class="swiper-slide">
                <img src="images/landing.png" alt="" srcset="">
            </div>
            <div class="swiper-slide">
                <img src="images/landing.png" alt="" srcset="">
            </div>
        </div>
        <div class="swiper-pagination"></div>
    </div>
     <div class=" my-5 flex justify-center w-full">
                    <div class=" flex w-full justify-between">
                    <h3 class="font-bold text-2xl"> مقترحة لك</h3>
                    <a href="/collections/shop">المزيد</a>

                </div>
            </div>
        <div class="flex w-full justify-center">
           <div class="lg:w-10/12 w-11/12">
       <div class=" grid lg:grid-cols-4 xl:grid-cols-4 2xl:grid-cols-4 content-center  
       place-items-center md:gap-2 md:grid-cols-3 gap-y-2  sm:gap-2 sm:grid-cols-2 lg:gap-16">
 
               {% for item in products %}
                    {{ product(title=item.title,priceAfterDiscount=item.priceAfterDiscount,price=item.price,imageUrl=item.images[0].imageUrl,slug=item.slug) }}
                {% endfor %}
             
       </div>
   </div>
        </div>

        </div>
    
    </div>
{% endblock %}

